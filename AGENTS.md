# AGENTS.md

Agent instructions for this repository live in [`CLAUDE.md`](CLAUDE.md). This
file exists so tools that look for `AGENTS.md` find them; the two must not
diverge, so this one carries no rules of its own.
