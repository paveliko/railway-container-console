# Tasks: `retired`

`R-TRACE` still runs in here: archiving records that the work finished, and the
trace is the evidence. A criterion no task reaches is an error here too.

- [x] **T-RT-1 Do the archived thing.** Result: a thing. Acceptance: V-RT-1. Verified by: `pnpm test`.
