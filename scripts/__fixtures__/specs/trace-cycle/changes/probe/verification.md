# Verification: `probe`

Deliberately wrong. Used by `check-specs.mjs --self-test`.

- V-PR-1 `unit` — As `V-PR-2` requires, the first thing is so.
- V-PR-2 `unit` — As `V-PR-1` requires, the second thing is so.
