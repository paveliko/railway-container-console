# Design: `probe`

Break-and-revert: write an undefined decision id — `example:D-API-9` — into a
design document, and an undefined criterion — `example:V-PR-99`. The check must
fail naming the file, line and identifier; revert.
