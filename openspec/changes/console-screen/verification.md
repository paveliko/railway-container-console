# Verification: `console-screen`

Prefix `V-SC-N`. Parent criteria cited by their `V-N`.

Each line ends with the run that covers it. First recorded 2026-09-15 against
the tree `vite-console` (PR #8) left, which verified its own `V-VC-N` and left
five criteria here with no run at all; those five — `V-SC-3` … `V-SC-8` — were
closed later the same day and now name their runs like the rest.

One of them was not merely unrun: `V-SC-8` described a check that did not
exist. Its line says what was found and what was done about it.

- V-SC-1 `unit` — `V-42`: one test per row of parent §1, asserting headline, detail line and control label from a `ContainerState` fixture. *Covered:* `apps/console/src/client/features/container-control/presentation.test.ts`, "the nine frames" — one case per row, asserting headline, detail and control label.
- V-SC-2 `unit` — `V-43`, `V-44`: disabled for `starting` / `stopping`; both controls enabled for `unknown`. *Covered:* `presentation.test.ts`, "starting — humanised status, control disabled", "stopping — no detail, control disabled" and "unknown — both controls, and the literal observation — V-44".
- V-SC-3 `unit` — `V-48`: with a fake `fetch` and a fake `EventSource`, the `GET` result renders before the first SSE event and the `EventSource` is constructed after the `GET` resolves. *Covered:* `ContainerPanel.test.tsx`, "constructs the EventSource only after the GET resolves — V-SC-3, V-48" — the read is held open on a promise the test releases, and `FakeEventSource.last` is asserted `undefined` while the read is in flight and defined once it answers, so the ordering is measured rather than assumed.
- V-SC-4 `unit` — `V-47`: an SSE event for `down` after `up` leaves no URL on screen. *Covered:* `ContainerPanel.test.tsx`, "leaves no URL on screen when down follows up — V-SC-4, V-47" — an `up` frame carrying a URL renders it as a link, and the `down` that follows leaves neither the link nor the host text anywhere on the card.
- V-SC-5 `unit` — `V-45`, `V-46`: a `409` renders *already starting* with no error styling; a `502` with `traceId` renders the error line containing it and leaves headline and control unchanged. *Covered:* `ContainerPanel.test.tsx`, "phrases a 409 as a notice and a 502 as an error — V-SC-5, V-45, V-46" (the notice is on the detail line and the `role="alert"` row is still the blank reserved line) and "renders a 502 with its traceId, headline and control unchanged — V-SC-5, V-46" (the alert reads exactly `The token is not permitted to do this · trace trace-abc`, and the headline and the control are what they were before the press).
- V-SC-6 `unit` — An SSE event whose body fails `containerStateSchema` is ignored and logged; the previous state stays rendered. *Covered:* `ContainerPanel.test.tsx`, "ignores an SSE frame that fails containerStateSchema, and logs it — V-SC-6" — both a well-formed body with a phase the contract does not have and a malformed one, each leaving the headline as it was, with `console.error` asserted; and a good frame afterwards still lands, so the stream is not poisoned by either.
- V-SC-7 `unit` — `V-49`: after mount, press and one event, `localStorage.length` and `sessionStorage.length` are 0 and `document.cookie` is empty (jsdom). *Covered:* `ContainerPanel.test.tsx`, "writes nothing to storage after mount, press and event — V-SC-7, V-49" — the measurement the criterion asks for, taken in jsdom after all three, rather than the grep for an absent API that would have passed without proving it.
- V-SC-8 `build` — `V-15`: the grep step of design §3 passes; it fails when the sentinel is deliberately imported into a client component (break-and-revert, logged). *Covered 2026-09-15, and the criterion was wrong about where.* This line used to say the grep was at `apps/console/vite.config.ts:29`. It was not: line 29 was a **comment** describing a grep that existed nowhere in the repository, and design §3 still specified the Next.js form of the step over `.next/static/`. Both are corrected — §3 is amended to the Vite form, and `scripts/check-bundle.mjs` is the step: it runs from `@repo/console`'s `build` script right after `vite build`, over every file under `dist/`, sourcemaps included. *Green:* `pnpm build` prints `Bundle check passed: 4 emitted file(s), 1 needle(s)`. *Red, logged in the PR:* importing `RAILWAY_GRAPHQL_ENDPOINT` into `ConnectionIndicator.tsx` fails `pnpm build` with exit 1, naming `assets/index-*.js` **and** `assets/index-*.js.map`; reverted, and green again. It is a script rather than a Vite plugin because `V-MW-9` allows the host one file and a check that greps for it must name it — design §3 records the two alternatives that were tried and dropped.
- V-SC-9 `build` — **Amended by `vite-console`:** the feature folder moved to `apps/console/src/client/features/` when `D-UI-5` made "the browser's code is this directory" one checkable sentence; the path here was left behind, while `V-SC-10` already uses the new one. No file under `apps/console/src/client/features/` imports `@repo/container-core` or `@repo/railway-client`; its only workspace imports are `@repo/contracts` and `@repo/ui` (grep — the same rule `V-MW-25` enforces from the workspace side). *Covered:* `scripts/check-boundaries.mjs` rule 4, in `pnpm check`.
- V-SC-10 `unit` — Every component under `apps/console/src/features/container-control/` that renders a button, badge, card or spinner does so through `@repo/ui`, not through a locally redeclared element (grep for `<button` in the feature folder returns nothing). *Covered:* `scripts/check-boundaries.mjs`, in `pnpm check`; `packages/ui/src/primitives.test.tsx` covers the primitives themselves.
