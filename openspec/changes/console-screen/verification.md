# Verification: `console-screen`

Prefix `V-SC-N`. Parent criteria cited by their `V-N`.

Each line ends with the run that covers it, recorded 2026-09-15 against the tree
`vite-console` (PR #8) left, or with the fact that none does. `vite-console`
verified its own `V-VC-N`; five criteria here were never picked up by that pass.

- V-SC-1 `unit` — `V-42`: one test per row of parent §1, asserting headline, detail line and control label from a `ContainerState` fixture. *Covered:* `apps/console/src/client/features/container-control/presentation.test.ts`, "the nine frames" — one case per row, asserting headline, detail and control label.
- V-SC-2 `unit` — `V-43`, `V-44`: disabled for `starting` / `stopping`; both controls enabled for `unknown`. *Covered:* `presentation.test.ts`, "starting — humanised status, control disabled", "stopping — no detail, control disabled" and "unknown — both controls, and the literal observation — V-44".
- V-SC-3 `unit` — `V-48`: with a fake `fetch` and a fake `EventSource`, the `GET` result renders before the first SSE event and the `EventSource` is constructed after the `GET` resolves. **No run.** `ContainerPanel.test.tsx` stubs both `fetch` and `EventSource` and asserts the first read renders, but nothing asserts the *ordering* — that the `EventSource` is constructed only after the `GET` resolves. `T-SC-1` is open on this.
- V-SC-4 `unit` — `V-47`: an SSE event for `down` after `up` leaves no URL on screen. **No run.** `presentation.test.ts` has "up — the URL as a link", but no case emits a `down` after an `up` and asserts the URL is gone. `T-SC-2` is open on this.
- V-SC-5 `unit` — `V-45`, `V-46`: a `409` renders *already starting* with no error styling; a `502` with `traceId` renders the error line containing it and leaves headline and control unchanged. **No run.** `messages.test.ts` has "phrases a 409 as information, not failure", which is the sentence, not the rendering; nothing renders a `502` with a `traceId` and asserts headline and control are unchanged. `T-SC-2` is open on this.
- V-SC-6 `unit` — An SSE event whose body fails `containerStateSchema` is ignored and logged; the previous state stays rendered. **No run.** No test emits an SSE body that fails `containerStateSchema`. `T-SC-1` is open on this.
- V-SC-7 `unit` — `V-49`: after mount, press and one event, `localStorage.length` and `sessionStorage.length` are 0 and `document.cookie` is empty (jsdom). **No run.** Nothing in `apps/console/src` reads or writes `localStorage`, `sessionStorage` or `document.cookie` on the client, but the absence of the API in the source is not the assertion the criterion makes, which is a jsdom measurement after mount, press and event. `T-SC-2` is open on this.
- V-SC-8 `build` — `V-15`: the grep step of design §3 passes; it fails when the sentinel is deliberately imported into a client component (break-and-revert, logged). **No run.** `apps/console/vite.config.ts:29` carries the `V-15` grep over the built assets, but the *Verified by* is a logged break-and-revert and none is logged. `T-SC-3` is open on this.
- V-SC-9 `build` — **Amended by `vite-console`:** the feature folder moved to `apps/console/src/client/features/` when `D-UI-5` made "the browser's code is this directory" one checkable sentence; the path here was left behind, while `V-SC-10` already uses the new one. No file under `apps/console/src/client/features/` imports `@repo/container-core` or `@repo/railway-client`; its only workspace imports are `@repo/contracts` and `@repo/ui` (grep — the same rule `V-MW-25` enforces from the workspace side). *Covered:* `scripts/check-boundaries.mjs` rule 4, in `pnpm check`.
- V-SC-10 `unit` — Every component under `apps/console/src/features/container-control/` that renders a button, badge, card or spinner does so through `@repo/ui`, not through a locally redeclared element (grep for `<button` in the feature folder returns nothing). *Covered:* `scripts/check-boundaries.mjs`, in `pnpm check`; `packages/ui/src/primitives.test.tsx` covers the primitives themselves.
