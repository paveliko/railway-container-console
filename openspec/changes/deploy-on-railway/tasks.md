# Tasks: `deploy-on-railway`

- [~] **T-DR-0 Usage limit.** *Owner.* Parent T-2.3, `V-56`. Acceptance: V-DR-5. Nothing below before this.
- [x] **T-DR-1.1 What Railway can and cannot be told.** Result: design §1 records, with source and date, that Config as Code is deprecated and closed to new services (cutoff 2026-12-01 for services already on it), the whole key set the deprecated file could ever have carried, and what `.railway/railway.ts` expresses instead; design §2 gives every settings row its disposition under each live mechanism. No configuration file is written — which one to write is `Q-OPS-8`. Acceptance: V-DR-7. Verified by: reading design §1 and §2. *Preparation only: this checks a document against Railway's documentation, not a deployment against Railway.*
- [~] **T-DR-1.2 The settings, on the service.** *Owner.* Result: the console service carries the build command, start command, watch paths and restart policy of design §2. Depends on: `Q-OPS-8`, T-DR-3. Acceptance: V-DR-8. Verified by: the dashboard.
- [~] **T-DR-2 Target service.** *Owner.* Parent T-7.1. Acceptance: `V-53`.
- [~] **T-DR-3 Console service.** *Owner deploys, agent prepares.* Parent T-7.2. The variables of parent design §3, `CONSOLE_PASSPHRASE` included and set (`D-SEC-2`); its value is chosen in the dashboard and delivered privately with the demo link, never committed — including never in a `.railway/railway.ts`, should `Q-OPS-8` pick one, where `preserve()` is the shape to use. Carries the settings of design §2 unless `Q-OPS-8` puts them in a file. Acceptance: V-DR-1, V-DR-2, V-DR-3, V-DR-6, `V-52`, `V-54`, `V-55`.
- [~] **T-DR-4 Cycle once, by hand.** *Owner.* Result: parent T-7.3 — the deployed console cycled once by hand, two tabs and a refresh mid-transition. Acceptance: V-50, V-51. Verified by: screenshots in the PR.
- [ ] **T-DR-5 Close `Q-OPS-3`.** Result: the register updated. Depends on: T-DR-3. Acceptance: V-DR-4.
- [ ] **T-DR-6 Paperwork.** Result: parent T-7.\* marked done here; archive. Depends on: T-DR-5.
