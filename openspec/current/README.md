# `current/`

What the console is meant to be, once it is decided.

Empty by design: the two questions that determine the shape of this thing —
what "spin up" and "spin down" mean in Railway's API (`Q-API-1`, `Q-API-2`) — are
still open, and a specification written before they are answered would be a guess
wearing a specification's clothes.
